import { NavLink } from "react-router-dom";

const Nav = () => {
  return (
    <div className="font-semibold">
      <NavLink to="/projects">Projects</NavLink>
    </div>
  );
};

export default Nav;
